# Optional AI companion proxy. Do not put Anthropic/OpenAI/API secrets in Expo public env vars.
# The proxy should keep provider API keys on the server and return JSON like {"reply":"..."}.
EXPO_PUBLIC_AI_PROXY_URL=
